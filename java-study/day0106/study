====================================
<컬렉션 프레임워크>
인터페이스 : 필드멤버(static final), 메서드 멤버(abstract method();)
List 인터페이스 : 순서가 있다. 중복허용(값), ArrayList<>, Vector<>, LinkedList, Stack, Queue 등이 있다.
set 인터페이스 : 순서가 없다. 중복을 허용하지 않는다. hashSet, TreeSet